{
  "name": "veilguard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Confidential spending policies for Safe treasuries, powered by iExec Nox",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "check": "node scripts/check-runtime.mjs && node scripts/check-deployments.mjs",
    "pretest": "npm run check",
    "test": "hardhat test",
    "test:server": "node --test server/test/*.test.mjs",
    "prebuild": "npm run check",
    "build": "hardhat build"
  },
  "devDependencies": {
    "@iexec-nox/nox-hardhat-plugin": "^0.1.0",
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.7",
    "@types/node": "^26.1.1",
    "hardhat": "^3.10.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@iexec-nox/nox-confidential-contracts": "^0.2.2",
    "@iexec-nox/nox-protocol-contracts": "^0.2.4",
    "@openzeppelin/contracts": "^5.6.1",
    "@safe-global/api-kit": "^5.0.1",
    "@safe-global/protocol-kit": "^8.0.4",
    "@iexec-nox/handle": "^0.1.0-beta.13"
  }
}
